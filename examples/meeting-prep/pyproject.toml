[project]
name = "meeting-prep"
version = "0.1.0"
description = "AI meeting prep briefing — researches a person and company, generates a one-page briefing with talking points"
requires-python = ">=3.11"
dependencies = [
    "anthropic",
]

[project.scripts]
briefing = "briefing:main"
