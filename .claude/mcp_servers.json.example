{
  "mcpServers": {
    "granola": {
      "command": "npx",
      "args": ["-y", "granola-mcp"],
      "_comment": "Optional. Pulls meeting transcripts into call_notes/ during /brainstorm. Remove this line and rename file to mcp_servers.json to enable."
    }
  }
}
